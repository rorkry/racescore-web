# Force npm instead of pnpm
[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
# Use npm install instead of npm ci to rebuild native modules for Linux
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
